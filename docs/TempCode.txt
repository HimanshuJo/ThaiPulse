package com.thaipulse.controller;

import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;

import java.util.List;
import java.util.Map;

@RestController
public class NewsController {

    @GetMapping("/news")
    public Map<String, Object> getDummyNews() {
        return Map.of(
            "status", "success",
            "data", List.of(
                Map.of(
                    "title", "Local News Testing Successful",
                    "summary", "Spring Boot with Oracle is now working locally.",
                    "timestamp", System.currentTimeMillis()
                ),
                Map.of(
                    "title", "Second Dummy Article",
                    "summary", "This is a placeholder for future real news.",
                    "timestamp", System.currentTimeMillis()
                )
            )
        );
    }
}
